### DATASET
DRIVE, STARE, CHAS_DB1, and HRF are all public datasets.

### 
The global context stream of DiDMamba-Net is initialized using pre-trained weights to accelerate convergence and enhance feature representation: vim_s_midclstok_ft_81p6acc.pth.

###
